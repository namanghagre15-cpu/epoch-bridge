{
  "name": "epoch-whatsapp-bridge",
  "version": "2.0.0",
  "description": "Epoch AGI WhatsApp Bridge — Render.com compatible",
  "main": "whatsapp_bridge.js",
  "scripts": {
    "start": "node whatsapp_bridge.js",
    "build": "npm install && npx puppeteer browsers install chrome"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "express": "^4.18.2",
    "form-data": "^4.0.0",
    "qrcode": "^1.5.3",
    "qrcode-terminal": "^0.12.0",
    "puppeteer": "^22.4.0",
    "whatsapp-web.js": "^1.23.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
